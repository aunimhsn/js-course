let fruits = ['apple', 'orange', 'banana', 'strawberry'];

fruits.sort(); // Sort the array alphabetically
fruits.shift(); // Remove the first element of an array and returns it
fruits.push('pear'); // Add a new element at the end of the array

// Multiple data type in the same array
let data = [22, 'green', ['pineapple', 'yellow']];

// .isArray()
let age = 22;
Array.isArray(age); // Return false
