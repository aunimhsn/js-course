let fruits = [
    ['banana', 'yellow'],
    ['apple', 'green'],
    ['orange', 'orange'],
    ['strawberry', 'red'],
];

let fruitName = 'apple';

/**
 * Please write a function returning the color of the fruit. 
 * --
 * 
 */
function getFruitColor(fruits, fruitName) {

    let color;

    fruits.forEach(fruit => {
        // Votre code...
    });

    return color;
}

console.log(getFruitColor());