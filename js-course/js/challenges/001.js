let fruits = [
    ['banana', 'yellow'],
    ['apple', 'green'],
    ['orange', 'orange'],
    ['strawberry', 'red'],
];

/**
 * Please write a function returning the color of the fruit. 
 * --
 * First parameter will be a string of any fruit like 'apple'.
 * Second parameter will be the above array.
 * 
 */

 fruits.forEach((element) => {
    
 });